name: Build and Deploy to Netlify
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Netlify
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: './dist' # Set your publish directory
          production-branch: 'master' # Set your production branch
          github-token: ${{ secrets.GITHUB_TOKEN }}
          deploy-message: "Deploy from GitHub Actions"
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.nfp_nCBT69x1GajYG9PoKNYrdips1PMds6GA889f }}
          NETLIFY_SITE_ID: ${{ secrets.dc9ac2d2-9aaa-4c8e-a44d-a72098fa88a8 }}
